model: orders
destination: pedramsslack-54791
config:
  format: message
  channel: C01NNMS72EB
  addedBody: |
    New Order on {{ order_date }} for {{ amount }}
schedule:
  type: manual
schedulePaused: false
